# Nexis Version Configuration
# ---------------------------
# This file pins the versions for core components to ensure reproducible builds.
# To update a version, change the value here and rebuild the Docker image.
# Ensure that any new versions are compatible with CUDA 12.x and the base image.

# ComfyUI Version (use a specific git tag)
export COMFYUI_VERSION="v0.3.48"

# FileBrowser Version
export FILEBROWSER_VERSION="2.23.0"