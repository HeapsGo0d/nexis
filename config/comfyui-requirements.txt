# ComfyUI additional dependencies
opencv-python-headless==4.8.1.78
scipy==1.11.4
scikit-image==0.22.0

# ─── Comfy-specific extras ─────────────────────────
huggingface_hub==0.25.*
transformers>=4.38.0,<5.0

# Runtime helpers required by ComfyUI / k-diffusion
trampoline==0.1.2
aiohttp==3.9.*

# aiohttp dependencies (to prevent version conflicts)
multidict==6.1.*
yarl==1.12.*
aiohappyeyeballs==2.4.*
propcache==0.2.*

# (xformers is installed separately in the Dockerfile via its CUDA wheel)